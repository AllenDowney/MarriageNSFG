# ---------------------------------------------------------------------------
# Survey data. NSFG and IPUMS microdata may not be redistributed, and it was
# purged from this repo's history on 2026-09-20. Do not add it back.
# Use scripts/download_nsfg.py to fetch it. See Task 4 on PROJECT_BOARD.md.
# ---------------------------------------------------------------------------
data/
*.dat
*.dat.gz
*.sas7bdat
*.dta
*.dta.gz

# Derived data products, regenerated by clean_nsfg
*.hdf
*.parquet
marriage_nsfg_*.csv
marriage_nsfg_*.csv.gz
FemMarriageData.csv
usa_00002.csv.gz
jun24pub.csv
h2.xlsx

# shelve databases written by the legacy notebooks
predictions.*
sf_map.*

# Notebooks are committed WITH outputs so they render on GitHub -- the data is
# not in the repo, so the stored outputs are the only way a reader sees results.
# The .md is still the editing surface; regenerate with `make notebooks` and
# `make execute`, and commit the .md and .ipynb together so they cannot drift.
.ipynb_checkpoints/

# Python
__pycache__/
*.py[cod]
*.so
.Python
env/
build/
dist/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Editors and OS
.vscode/
.idea/
*~
.DS_Store

# Test / coverage
htmlcov/
.coverage
coverage.xml
nsfg.egg-info

# Jupyter Book build output
jb/_build/
